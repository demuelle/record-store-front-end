function Destructure({ heroName, hp, mana }) {
  return (
    <div>
      <h2>Hero: {heroName}</h2>
      <ul>
        <li>HP: {hp}</li>
        <li>Mana: {mana}</li>
      </ul>
    </div>
  );
}

export default Destructure;
