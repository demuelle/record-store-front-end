import { render, screen } from '@testing-library/react';
import App from './App';

test('renders Game Store link', () => {
  render(<App />);
  expect(screen.getByText('Game Store')).toBeInTheDocument();
});
