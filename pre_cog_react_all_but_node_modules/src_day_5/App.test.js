import { render, screen } from '@testing-library/react';
import App from './App';

test('renders "No Encounters"', () => {
  render(<App />);
  const nope = screen.getByText(/No Encounters/i);
  expect(nope).toBeInTheDocument();
});
